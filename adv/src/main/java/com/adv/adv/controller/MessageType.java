package com.adv.adv.controller;

public enum MessageType {
    CHAT,  // Represents a regular chat message
    JOIN,  // Represents a user joining the chat
    LEAVE  // Represents a user leaving the chat
}
